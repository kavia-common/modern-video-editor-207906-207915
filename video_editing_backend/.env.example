# Backend configuration (example only; do NOT commit secrets)
# Required:
# DATABASE_URL should use SQLAlchemy async dialect:
# postgresql+asyncpg://appuser:dbuser123@localhost:5000/myapp
DATABASE_URL=

# Optional compatibility fallback:
# DB_CONNECTION_STRING=

# CORS (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000
ALLOWED_METHODS=GET,POST,PUT,DELETE,PATCH,OPTIONS
ALLOWED_HEADERS=Content-Type,Authorization,X-Requested-With
CORS_MAX_AGE=3600

# Upload stub directory (local disk)
UPLOAD_DIR=uploads
